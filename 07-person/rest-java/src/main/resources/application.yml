server:
  port: ${PORT:8100}


    
spring:    
  datasource:
    url: jdbc:mysql://localhost:3310/restjava?useSSL=false&allowPublicKeyRetrieval=true&
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show_sql: true

